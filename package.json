{
  "name": "cloudflare-quota-monitor",
  "version": "0.3.6",
  "private": true,
  "description": "Automated checks for the Cloudflare Free Quota Monitor extension.",
  "scripts": {
    "build:site": "node scripts/build-site.mjs",
    "deploy:site": "npm run build:site && (npx wrangler pages project create cloudflare-quota-monitor --production-branch main || true) && npx wrangler pages deploy dist/site --project-name cloudflare-quota-monitor --branch main",
    "package": "node scripts/package-extension.mjs",
    "test": "node --test tests/*.test.js"
  }
}
